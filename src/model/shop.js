class Shop {
    constructor (id, name, description, longitude, latitude) {
        this.id = id,
        this.name = name,
        this.description = description,
        this.longitude = longitude,
        this.latitude = latitude
    }
}

export default new Shop();